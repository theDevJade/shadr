# Gradle 8.10.2 cannot run its daemon on Java 25. Run it on a Java 21 JDK; the toolchain
# block in build.gradle.kts pins what the code compiles against (Java 21, Paper 1.21.8).
org.gradle.jvmargs=-Xmx2g
kotlin.code.style=official
